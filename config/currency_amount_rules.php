<?php

declare(strict_types=1);

/**
 * Правила ввода суммы (decimal) в UI / API по валютам.
 *
 * Важно: это НЕ обязательно соответствует on-chain precision.
 * Например, USDT (TRC20) on-chain имеет 6 знаков, но в инвойсах
 * мы можем ограничить ввод более простым форматом (например, 2 знака).
 *
 * Как расширять:
 * - добавьте валюту в `App\Enums\Currency`
 * - добавьте её правила здесь
 * - фронт автоматически получит их через Inertia shared props
 */
return [
    'default' => [
        // Сколько знаков после точки разрешено в вводе суммы инвойса
        'invoice_decimals' => 6,
        // Пример для placeholder/подсказок
        'example' => '12.123456',
        // Разделитель дробной части в UI (в проекте используем точку)
        'decimal_separator' => '.',
    ],

    'usdt' => [
        'invoice_decimals' => 2,
        'example' => '12.50',
        'decimal_separator' => '.',
    ],
];


